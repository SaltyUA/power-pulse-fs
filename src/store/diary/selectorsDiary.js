export const getDiaryInfo = (state) => state.diary.diaryInfo;

export const getDateInfo = (state) => state.diary.choiceDate;

// export const getExercises = (state) => state.diary.exercises;

// export const getProducts = (state) => state.diary.products;